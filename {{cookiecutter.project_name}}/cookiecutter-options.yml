default_context:
    project_name: "{{ cookiecutter.project_name }}"
    app_name: "{{ cookiecutter.app_name }}"
    python_version: "{{ cookiecutter.python_version }}"
    admin_user_username: "{{ cookiecutter.admin_user_username }}"
    admin_user_email: "{{ cookiecutter.admin_user_email }}"
    admin_user_password: "{{ cookiecutter.admin_user_password }}"
    use_celery: "{{ cookiecutter.use_celery }}"
    wsgi_server: "{{ cookiecutter.wsgi_server }}"
